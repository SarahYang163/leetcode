import random

# l = [1, 2, 3, 4, 5]
# new_l = map(lambda x: x ** 2, l)
# print(new_l)
#
# a = [_ for _ in new_l if _ > 10]
# print(a)
print(random.randint(1, 3))  # 包含1和3
print(random.randrange(1, 3))  # 包含1不包含3
print(random.random())  # 包含0不包含1
